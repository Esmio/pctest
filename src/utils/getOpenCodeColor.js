
import { bgGray, accentCinnabar, labelBlue, accentTeal } from '../styles/variables.less';


export function getOpenCodeColor(openCode) {
  switch (openCode) {
    default:
      return bgGray;
  }
}
